{
  "//": [
    "Playwright transpiles the specs with esbuild and never type-checks them, so",
    "this file is what an editor reads and what `make e2e-typecheck` runs --",
    "which `make e2e` requires before it starts a stack.",
    "",
    "DOM is in lib because a spec is two languages in one file: the outer half is",
    "Node, and everything inside page.evaluate() is browser code. window, URL,",
    "setTimeout and import.meta.url all come from here.",
    "",
    "'node' is in types for the other half — process, Buffer and the node:*",
    "imports — which is what @types/node and typescript are devDependencies of",
    "this package for. Neither ships to anything; they exist so the specs can be",
    "checked rather than only run."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["@playwright/test", "node"]
  },
  "include": ["**/*.ts"]
}
